[project]
name = "cocotb-hash_wrapper"
version = "0.1.0"
dependencies = [
    "cocotb >= 2.0.0",
    "cocotbext-ofm @ ${NDK_FPGA_COCOTBEXT_OFM_URL}",
    "setuptools",
    "spookyhash",
    "siphash-cffi",
]

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"
