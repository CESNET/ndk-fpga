[project]
name = "cocotb-mfb-checksum-l3l4-test"
version = "0.1.0"
dependencies = [
    "cocotbext-ofm[nfb] @ ${NDK_FPGA_COCOTBEXT_OFM_URL}",
    "setuptools",
    "scapy",
    "ipaddress",
]

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"
