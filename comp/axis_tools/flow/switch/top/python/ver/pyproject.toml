[project]
name = "cocotb-axis_switch"
version = "0.1.0"
dependencies = [
    "cocotb == 1.9.2",
    "cocotbext-ofm[nfb] @ ${NDK_FPGA_COCOTBEXT_OFM_URL}",
    "cocotbext-axi",
    "setuptools",
    "scapy",
]

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"
